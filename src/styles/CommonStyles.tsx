import { StyleSheet } from "react-native";

export const CommonStyles = StyleSheet.create({
  appContainer: {
    flex: 1,
    fontSize: 16,
    paddingHorizontal: 20,
    backgroundColor: "#fff",
  },
});
